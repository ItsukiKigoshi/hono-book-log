{
  "name": "hono-book-log",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "bun --filter book-log-api dev",
    "dev:app": "bun --filter book-log-app dev",
    "dev": "bun --filter \"*\" dev"
  }
}